##########################
 COMMAND
      write [filename]
 DESCRIPTION
      Writes netlist of the current state of design (if successful 'build' was).
 ARGUMENTS
      [filename] - name of the file to be written.
                   '${WORKSPACE_NAME}_netlist.v' is used if omitted.
